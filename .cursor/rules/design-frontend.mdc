---
description: Multi-agent territory — Design/frontend (UI components, CSS/Tailwind, existing page structure)
globs: components/**/*.tsx,app/**/*.tsx,app/**/*.css,app/globals.css
alwaysApply: false
---

# Dominio: Diseño / Frontend

Rama tipo: `cursor/design-<tema>`

## Puede tocar

- Componentes visuales
- CSS / Tailwind
- Estructura de páginas **ya existentes**
- Integraciones desde Stitch/Figma **adaptadas** al stack real (Next.js / Tailwind / componentes del repo)

## No puede tocar

- Endpoints API (`app/api/**`)
- Lógica de autenticación
- Esquema de base de datos

## Regla especial — Stitch / Figma

- Adaptar al stack y patrones existentes del repo.
- **Nunca** pegar código externo literal sin adaptarlo.

## Gobernanza (obligatoria)

- Una rama por tarea; el nombre debe coincidir con el contenido real del primer commit.
- Antes de trabajar en paralelo: verificar solapamiento de archivos con otros agentes.
- Prioridades solo desde el Checklist Maestro BeTacora.
- **`npm run build` limpio ANTES de cualquier commit.**
- Commit en rama propia; nunca push directo a `main`. Push a producción solo con confirmación explícita del humano.

## ZONA DE EXCLUSIÓN TOTAL (todos los agentes)

Sin autorización explícita caso por caso, **prohibido** tocar:

- Esquema de Supabase / políticas RLS (solo lectura y diagnóstico)
- Lógica de autenticación / OAuth
- Cualquier cosa relacionada con pagos o dinero real
- Variables de entorno de producción (Vercel)

## Nota de territorio

Si el cambio es principalmente cuestionario / Explorar / perfil DNA (producto), ceder a `cursor/product-*` y no competir por los mismos archivos.
